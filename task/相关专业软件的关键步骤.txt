1.用psql执行建表SQL，建立目录状态、原始事件、事件判决和批次回执表
2.装载基线目录与变更事件，依靠event_id唯一约束处理重复投递
3.在批次函数中按商品和位置顺序计算winner，记录同批落选与迟到事件
4.用严格三元位置比较判定APPLY，并将比较前水位写入事件判决表
5.以当前批APPLY winner作为MERGE来源，更新活跃商品或写入删除墓碑
6.按合同批次顺序执行函数，导出目录状态、可用目录、事件判决和批次回执
